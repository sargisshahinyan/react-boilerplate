import React from 'react';
import ReactDOM from 'react-dom';

import Styles from './styles.module.scss';

ReactDOM.render(
  <h1 className={Styles.class}>Seems working!!!</h1>,
  document.getElementById('root'),
);
